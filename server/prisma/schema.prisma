generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  // @default(uuid()) vai gerar para cada usuario um id unico universal  
  // @id significa a chave primaria 
  id   String @id @default(uuid())
  name String
}
